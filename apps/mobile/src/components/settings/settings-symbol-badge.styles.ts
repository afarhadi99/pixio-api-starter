import { StyleSheet } from 'react-native';

export const settingsSymbolBadgeStyles = StyleSheet.create({
  shell: {
    alignItems: 'center',
    justifyContent: 'center',
    borderCurve: 'continuous',
  },
});
