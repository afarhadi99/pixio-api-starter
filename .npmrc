# Expo / React Native require a hoisted node_modules layout to resolve native modules
node-linker=hoisted
# Allow installing peer deps automatically
auto-install-peers=true
strict-peer-dependencies=false
